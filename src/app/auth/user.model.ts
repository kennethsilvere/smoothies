export class User {
    // firstName: string;
    // lastName: string;
    // email: string;
    // birthday: Date;
    // password: string;

    constructor(public firstName: string, public lastName: string, public email: string, public birthday: Date, public password: string){}
}